{
  "name": "backend",
  "version": "0.0.1",
  "description": "Backend generator for AI Skills (Python + Node.js wrapper)",
  "type": "module",
  "private": true,
  "scripts": {
    "generate": "python python/main.py",
    "generate:dry": "python python/main.py --dry-run",
    "test": "python -m pytest python/tests/",
    "lint": "black python/ --check",
    "format": "black python/"
  },
  "devDependencies": {}
}
